# AUTO-GENERATED by scripts/generate.mjs from spec/endpoints.json DO NOT EDIT.
# Regenerate with: node scripts/generate.mjs
from ._http import enc


class AuthResource:
    """Auth Registration, password + passwordless sign-in, token refresh, 2FA and verification."""

    def __init__(self, client):
        self._c = client

    def register(self, body=None, **opts):
        """Register a new user with email + password."""
        return self._c._request("POST", "/api/v1/auth/register", body=body, **opts)

    def login(self, body=None, **opts):
        """Sign in with email + password; returns the JWT pair (or a 2FA challenge)."""
        return self._c._request("POST", "/api/v1/auth/login", body=body, **opts)

    def login_verify2fa(self, body=None, **opts):
        """Complete a login that returned a 2FA challenge."""
        return self._c._request("POST", "/api/v1/auth/2fa-verify", body=body, **opts)

    def refresh(self, body=None, **opts):
        """Exchange a refresh token for a fresh access/refresh pair."""
        return self._c._request("POST", "/api/v1/auth/refresh", body=body, **opts)

    def logout(self, body=None, **opts):
        """Revoke a refresh token."""
        return self._c._request("POST", "/api/v1/auth/logout", body=body, **opts)

    def forgot_password(self, body=None, **opts):
        """Email a password-reset link."""
        return self._c._request("POST", "/api/v1/auth/forgot-password", body=body, **opts)

    def reset_password(self, body=None, **opts):
        """Set a new password using a reset token."""
        return self._c._request("POST", "/api/v1/auth/reset-password", body=body, **opts)

    def request_token(self, body=None, **opts):
        """Passwordless: email a 6-digit login code."""
        return self._c._request("POST", "/api/v1/auth/token/request", body=body, **opts)

    def exchange_token(self, body=None, **opts):
        """Passwordless: swap an emailed code for the JWT pair."""
        return self._c._request("POST", "/api/v1/auth/token/exchange", body=body, **opts)

    def verify_otp(self, body=None, **opts):
        """Verify a one-time passcode."""
        return self._c._request("POST", "/api/v1/auth/verify-otp", body=body, **opts)

    def verify_email(self, body=None, **opts):
        """Confirm an email address from a verification token."""
        return self._c._request("POST", "/api/v1/auth/verify-email", body=body, **opts)

    def verify_phone(self, body=None, **opts):
        """Confirm a phone number from an SMS code."""
        return self._c._request("POST", "/api/v1/auth/verify-phone", body=body, **opts)

    def login_oauth(self, body=None, **opts):
        """Sign in with a third-party OAuth identity token."""
        return self._c._request("POST", "/api/v1/auth/login/oauth", body=body, **opts)

    def enable2fa(self, body=None, **opts):
        """Begin enrolling TOTP two-factor auth."""
        return self._c._request("POST", "/api/v1/auth/2fa/enable", body=body, **opts)

    def verify2fa(self, body=None, **opts):
        """Confirm a TOTP code to finish 2FA enrollment."""
        return self._c._request("POST", "/api/v1/auth/2fa/verify", body=body, **opts)


class UsersResource:
    """Users (Buyer) The authenticated account: profile, wallet, tickets, orders, refunds, reports, messaging and notifications."""

    def __init__(self, client):
        self._c = client

    def me(self, query=None, **opts):
        """Current user profile."""
        return self._c._request("GET", "/api/v1/users/me", query=query, **opts)

    def update_me(self, body=None, **opts):
        """Update the current user profile."""
        return self._c._request("PUT", "/api/v1/users/me", body=body, **opts)

    def orders(self, query=None, **opts):
        """List the buyer's orders."""
        return self._c._request("GET", "/api/v1/users/me/orders", query=query, **opts)

    def tickets(self, query=None, **opts):
        """List the buyer's tickets across all organizers."""
        return self._c._request("GET", "/api/v1/users/me/tickets", query=query, **opts)

    def delete_account(self, body=None, **opts):
        """Close the account."""
        return self._c._request("DELETE", "/api/v1/users/me", body=body, **opts)

    def change_password(self, body=None, **opts):
        """Change the account password."""
        return self._c._request("POST", "/api/v1/users/me/password", body=body, **opts)

    def activity(self, query=None, **opts):
        """Recent account activity."""
        return self._c._request("GET", "/api/v1/users/me/activity", query=query, **opts)

    def login_info(self, query=None, **opts):
        """Last-login metadata."""
        return self._c._request("GET", "/api/v1/users/me/login-info", query=query, **opts)

    def twofa_status(self, query=None, **opts):
        """Whether 2FA is enabled."""
        return self._c._request("GET", "/api/v1/users/me/2fa/status", query=query, **opts)

    def twofa_setup(self, body=None, **opts):
        """Start 2FA setup (returns the TOTP secret/QR)."""
        return self._c._request("POST", "/api/v1/users/me/2fa/setup", body=body, **opts)

    def twofa_enable(self, body=None, **opts):
        """Enable 2FA after verifying a code."""
        return self._c._request("POST", "/api/v1/users/me/2fa/enable", body=body, **opts)

    def twofa_disable(self, body=None, **opts):
        """Disable 2FA."""
        return self._c._request("POST", "/api/v1/users/me/2fa/disable", body=body, **opts)

    def create_refund(self, body=None, **opts):
        """Submit a refund request for a ticket."""
        return self._c._request("POST", "/api/v1/users/me/refunds", body=body, **opts)

    def refunds(self, query=None, **opts):
        """List the buyer's refund requests."""
        return self._c._request("GET", "/api/v1/users/me/refunds", query=query, **opts)

    def withdraw_refund(self, id, body=None, **opts):
        """Withdraw a pending refund request."""
        return self._c._request("POST", "/api/v1/users/me/refunds/" + enc(id) + "/withdraw", body=body, **opts)

    def create_report(self, body=None, **opts):
        """File a report against an event or organizer."""
        return self._c._request("POST", "/api/v1/users/me/reports", body=body, **opts)

    def reports(self, query=None, **opts):
        """List the buyer's filed reports."""
        return self._c._request("GET", "/api/v1/users/me/reports", query=query, **opts)

    def messages(self, query=None, **opts):
        """Message threads with organizers."""
        return self._c._request("GET", "/api/v1/users/me/messages", query=query, **opts)

    def message_thread(self, thread_id, query=None, **opts):
        """A single message thread."""
        return self._c._request("GET", "/api/v1/users/me/messages/" + enc(thread_id), query=query, **opts)

    def send_ticket_message(self, ticket_id, body=None, **opts):
        """Message the organizer of a ticket."""
        return self._c._request("POST", "/api/v1/users/me/tickets/" + enc(ticket_id) + "/message", body=body, **opts)

    def notifications(self, query=None, **opts):
        """In-app notifications."""
        return self._c._request("GET", "/api/v1/users/me/notifications", query=query, **opts)

    def notifications_unread_count(self, query=None, **opts):
        """Count of unread notifications."""
        return self._c._request("GET", "/api/v1/users/me/notifications/unread-count", query=query, **opts)

    def mark_notifications_read(self, body=None, **opts):
        """Mark notifications as read."""
        return self._c._request("POST", "/api/v1/users/me/notifications/read", body=body, **opts)

    def update_avatar(self, body=None, **opts):
        """Update the account avatar."""
        return self._c._request("PUT", "/api/v1/users/me/avatar", body=body, **opts)

    def update_notification_settings(self, body=None, **opts):
        """Update notification preferences."""
        return self._c._request("PUT", "/api/v1/users/me/notifications/settings", body=body, **opts)


class SavedSearchesResource:
    """Saved searches The buyer's saved discovery searches."""

    def __init__(self, client):
        self._c = client

    def list(self, query=None, **opts):
        """List saved searches."""
        return self._c._request("GET", "/api/v1/users/me/saved-searches", query=query, **opts)

    def create(self, body=None, **opts):
        """Save a search."""
        return self._c._request("POST", "/api/v1/users/me/saved-searches", body=body, **opts)

    def delete(self, id, body=None, **opts):
        """Delete a saved search."""
        return self._c._request("DELETE", "/api/v1/users/me/saved-searches/" + enc(id), body=body, **opts)


class DataExportResource:
    """Data export GDPR-style export of the account's data."""

    def __init__(self, client):
        self._c = client

    def get(self, query=None, **opts):
        """Export all of the account's data."""
        return self._c._request("GET", "/api/v1/users/me/export", query=query, **opts)


class EventsResource:
    """Events (Public) Public event discovery rails and per-event read panels."""

    def __init__(self, client):
        self._c = client

    def list(self, query=None, **opts):
        """List / search public events (cursor-paginated)."""
        return self._c._request("GET", "/api/v1/events", query=query, **opts)

    def trending(self, query=None, **opts):
        """Trending events."""
        return self._c._request("GET", "/api/v1/events/trending", query=query, **opts)

    def categories(self, query=None, **opts):
        """Event categories with counts."""
        return self._c._request("GET", "/api/v1/events/categories", query=query, **opts)

    def nearby(self, query=None, **opts):
        """Events near a lat/lng or city."""
        return self._c._request("GET", "/api/v1/events/nearby", query=query, **opts)

    def new_this_week(self, query=None, **opts):
        """Recently published events."""
        return self._c._request("GET", "/api/v1/events/new-this-week", query=query, **opts)

    def ending_soon(self, query=None, **opts):
        """Events with sales ending soon."""
        return self._c._request("GET", "/api/v1/events/ending-soon", query=query, **opts)

    def free(self, query=None, **opts):
        """Free events."""
        return self._c._request("GET", "/api/v1/events/free", query=query, **opts)

    def recommended(self, query=None, **opts):
        """Personalized recommendations."""
        return self._c._request("GET", "/api/v1/events/recommended", query=query, **opts)

    def schedule(self, id, query=None, **opts):
        """An event's session schedule."""
        return self._c._request("GET", "/api/v1/events/" + enc(id) + "/schedule", query=query, **opts)

    def organizer(self, id, query=None, **opts):
        """Public organizer profile for an event."""
        return self._c._request("GET", "/api/v1/events/" + enc(id) + "/organizer", query=query, **opts)

    def faq(self, id, query=None, **opts):
        """An event's FAQ."""
        return self._c._request("GET", "/api/v1/events/" + enc(id) + "/faq", query=query, **opts)

    def related(self, id, query=None, **opts):
        """Related events."""
        return self._c._request("GET", "/api/v1/events/" + enc(id) + "/related", query=query, **opts)

    def express_interest(self, id, body=None, **opts):
        """Register interest in an event."""
        return self._c._request("POST", "/api/v1/events/" + enc(id) + "/interest", body=body, **opts)

    def issue(self, event_id, body=None, **opts):
        """Externally issue a ticket for an event (integrator mode)."""
        return self._c._request("POST", "/api/v1/events/" + enc(event_id) + "/issue", body=body, **opts)

    def get(self, slug, query=None, **opts):
        """Get a public event by slug."""
        return self._c._request("GET", "/api/v1/events/" + enc(slug), query=query, **opts)

    def tickets(self, id, query=None, **opts):
        """List an event's purchasable ticket types."""
        return self._c._request("GET", "/api/v1/events/" + enc(id) + "/tickets", query=query, **opts)


class TicketsResource:
    """Tickets Ticket QR/PDF, peer-to-peer transfers, promo validation and wallet passes."""

    def __init__(self, client):
        self._c = client

    def validate_promo(self, body=None, **opts):
        """Validate a promo code against a cart."""
        return self._c._request("POST", "/api/v1/tickets/promo/validate", body=body, **opts)

    def qr(self, id, query=None, **opts):
        """Current rotating QR payload for a ticket (JSON)."""
        return self._c._request("GET", "/api/v1/tickets/" + enc(id) + "/qr", query=query, **opts)

    def pdf(self, id, query=None, **opts):
        """Ticket PDF (application/pdf bytes)."""
        return self._c._request("GET", "/api/v1/tickets/" + enc(id) + "/pdf", query=query, raw=True, **opts)

    def transfer(self, id, body=None, **opts):
        """Initiate a peer-to-peer transfer; the recipient gets a claim link."""
        return self._c._request("POST", "/api/v1/tickets/" + enc(id) + "/transfer", body=body, **opts)

    def revoke_transfer(self, transfer_id, body=None, **opts):
        """Revoke a still-pending transfer (initiator only)."""
        return self._c._request("POST", "/api/v1/tickets/transfers/" + enc(transfer_id) + "/revoke", body=body, **opts)

    def get_transfer(self, token, query=None, **opts):
        """Inspect a pending transfer by claim token."""
        return self._c._request("GET", "/api/v1/tickets/transfers/claim/" + enc(token), query=query, **opts)

    def claim_transfer(self, token, body=None, **opts):
        """Claim a transfer; rewrites the ticket holder to the recipient."""
        return self._c._request("POST", "/api/v1/tickets/transfers/claim/" + enc(token), body=body, **opts)

    def wallet_pass(self, id, query=None, **opts):
        """Google/Apple wallet 'Save to Wallet' link (JSON)."""
        return self._c._request("GET", "/api/v1/tickets/" + enc(id) + "/wallet-pass", query=query, **opts)

    def list_by_event(self, event_id, query=None, **opts):
        """List ticket types for an event (legacy /ticket-types mount)."""
        return self._c._request("GET", "/api/v1/tickets/" + enc(event_id), query=query, **opts)


class OrdersResource:
    """Orders Carted checkout: create, pay, cancel, invoice."""

    def __init__(self, client):
        self._c = client

    def create(self, body=None, **opts):
        """Create an order (reserves inventory)."""
        return self._c._request("POST", "/api/v1/orders", body=body, **opts)

    def get(self, id, query=None, **opts):
        """Order detail."""
        return self._c._request("GET", "/api/v1/orders/" + enc(id), query=query, **opts)

    def cancel(self, id, body=None, **opts):
        """Cancel an order and release its hold."""
        return self._c._request("POST", "/api/v1/orders/" + enc(id) + "/cancel", body=body, **opts)

    def pay(self, id, body=None, **opts):
        """Initiate payment for an order (nowpayments | paystack | flutterwave)."""
        return self._c._request("POST", "/api/v1/orders/" + enc(id) + "/pay", body=body, **opts)

    def invoice(self, id, query=None, **opts):
        """Order receipt PDF (application/pdf bytes)."""
        return self._c._request("GET", "/api/v1/orders/" + enc(id) + "/invoice", query=query, raw=True, **opts)


class PaymentsResource:
    """Payments Payment intent creation, verification, method discovery, and inbound provider webhooks."""

    def __init__(self, client):
        self._c = client

    def verify(self, body=None, **opts):
        """Confirm a charge with the provider and complete the order (idempotent)."""
        return self._c._request("POST", "/api/v1/payments/verify", body=body, **opts)

    def get(self, order_id, query=None, **opts):
        """Payment status for an order."""
        return self._c._request("GET", "/api/v1/payments/" + enc(order_id), query=query, **opts)

    def initiate(self, body=None, **opts):
        """Create a payment intent."""
        return self._c._request("POST", "/api/v1/payments/initiate", body=body, **opts)

    def methods(self, query=None, **opts):
        """Available payment methods for a currency."""
        return self._c._request("GET", "/api/v1/payments/methods", query=query, **opts)

    def crypto_currencies(self, query=None, **opts):
        """Supported crypto currencies (NOWPayments)."""
        return self._c._request("GET", "/api/v1/payments/crypto/currencies", query=query, **opts)

    def webhook_nowpayments(self, body=None, **opts):
        """Inbound NOWPayments webhook (provider callback; not for client use)."""
        return self._c._request("POST", "/api/v1/payments/webhook/nowpayments", body=body, **opts)

    def webhook_paystack(self, body=None, **opts):
        """Inbound Paystack webhook (provider callback; not for client use)."""
        return self._c._request("POST", "/api/v1/payments/webhook/paystack", body=body, **opts)

    def webhook_flutterwave(self, body=None, **opts):
        """Inbound Flutterwave webhook (provider callback; not for client use)."""
        return self._c._request("POST", "/api/v1/payments/webhook/flutterwave", body=body, **opts)


class CheckinResource:
    """Check-in Gate scanning, offline manifests, batch sync, live stats and CSV export."""

    def __init__(self, client):
        self._c = client

    def scan(self, body=None, **opts):
        """Validate a QR / short-code ticket at a gate."""
        return self._c._request("POST", "/api/v1/checkin/scan", body=body, **opts)

    def batch(self, body=None, **opts):
        """Flush a queue of scans captured offline."""
        return self._c._request("POST", "/api/v1/checkin/batch", body=body, **opts)

    def manual(self, id, body=None, **opts):
        """Manually check in an attendee."""
        return self._c._request("POST", "/api/v1/checkin/event/" + enc(id) + "/manual", body=body, **opts)

    def manifest(self, id, query=None, **opts):
        """Offline manifest (ticket hashes + statuses); pass ?since for a delta."""
        return self._c._request("GET", "/api/v1/checkin/event/" + enc(id) + "/manifest", query=query, **opts)

    def stats(self, id, query=None, **opts):
        """Live check-in stats snapshot."""
        return self._c._request("GET", "/api/v1/checkin/event/" + enc(id) + "/stats", query=query, **opts)

    def register_device(self, body=None, **opts):
        """Register a scanning device."""
        return self._c._request("POST", "/api/v1/checkin/device/register", body=body, **opts)

    def live_url(self, id, query=None):
        """Server-Sent Events stream of live check-in stats. (SSE returns the stream URL)."""
        return self._c._url("/api/v1/checkin/event/" + enc(id) + "/live", query)

    def gate(self, id, gate, query=None, **opts):
        """Per-gate check-in stats."""
        return self._c._request("GET", "/api/v1/checkin/event/" + enc(id) + "/gate/" + enc(gate), query=query, **opts)

    def export(self, id, query=None, **opts):
        """Check-in log CSV (text/csv bytes)."""
        return self._c._request("GET", "/api/v1/checkin/event/" + enc(id) + "/export", query=query, raw=True, **opts)


class ScanResource:
    """Scanner-token check-in Passwordless gate scanning using a short-lived scanner token (the /scan kiosk surface)."""

    def __init__(self, client):
        self._c = client

    def exchange(self, body=None, **opts):
        """Exchange a scanner token for a scoped session."""
        return self._c._request("POST", "/api/v1/checkin-token/exchange", body=body, **opts)

    def session(self, query=None, **opts):
        """Current scanner session (event + gate context)."""
        return self._c._request("GET", "/api/v1/checkin-token/me", query=query, **opts)

    def scan(self, body=None, **opts):
        """Validate a ticket with the scanner session."""
        return self._c._request("POST", "/api/v1/checkin-token/scan", body=body, **opts)

    def manifest(self, query=None, **opts):
        """Offline manifest for the scanner session."""
        return self._c._request("GET", "/api/v1/checkin-token/manifest", query=query, **opts)

    def batch(self, body=None, **opts):
        """Flush offline scans for the scanner session."""
        return self._c._request("POST", "/api/v1/checkin-token/batch", body=body, **opts)


class SearchResource:
    """Search Full-text + faceted event search."""

    def __init__(self, client):
        self._c = client

    def query(self, query=None, **opts):
        """Full-text + faceted search."""
        return self._c._request("GET", "/api/v1/search", query=query, **opts)

    def suggest(self, query=None, **opts):
        """Type-ahead suggestions."""
        return self._c._request("GET", "/api/v1/search/suggest", query=query, **opts)

    def trending(self, query=None, **opts):
        """Trending search terms."""
        return self._c._request("GET", "/api/v1/search/trending", query=query, **opts)

    def popular(self, city, query=None, **opts):
        """Popular searches in a city."""
        return self._c._request("GET", "/api/v1/search/popular/" + enc(city), query=query, **opts)


class MediaResource:
    """Media Image/asset uploads and the stable /media/:id resolver."""

    def __init__(self, client):
        self._c = client


class OrganizerResource:
    """Organizer Authenticated organizer surface: organizations, members, events, ticket types, schedules, sections, promo codes, attendees, payouts, refunds, reports and messaging."""

    def __init__(self, client):
        self._c = client

    def setup(self, body=None, **opts):
        """Bootstrap an organizer account + first organization."""
        return self._c._request("POST", "/api/v1/organizer/setup", body=body, **opts)

    def me(self, query=None, **opts):
        """Current organizer context (orgs + memberships)."""
        return self._c._request("GET", "/api/v1/organizer/me", query=query, **opts)

    def create_organization(self, body=None, **opts):
        """Create an organization."""
        return self._c._request("POST", "/api/v1/organizer/organizations", body=body, **opts)

    def get_organization(self, org_id, query=None, **opts):
        """Organization detail."""
        return self._c._request("GET", "/api/v1/organizer/organizations/" + enc(org_id), query=query, **opts)

    def update_organization(self, org_id, body=None, **opts):
        """Update an organization."""
        return self._c._request("PUT", "/api/v1/organizer/organizations/" + enc(org_id), body=body, **opts)

    def set_organization_status(self, org_id, body=None, **opts):
        """Change an organization's status."""
        return self._c._request("PUT", "/api/v1/organizer/organizations/" + enc(org_id) + "/status", body=body, **opts)

    def members(self, org_id, query=None, **opts):
        """List organization members."""
        return self._c._request("GET", "/api/v1/organizer/organizations/" + enc(org_id) + "/members", query=query, **opts)

    def invite(self, org_id, body=None, **opts):
        """Invite a member."""
        return self._c._request("POST", "/api/v1/organizer/organizations/" + enc(org_id) + "/invites", body=body, **opts)

    def resend_invite(self, org_id, member_id, body=None, **opts):
        """Resend a member invite."""
        return self._c._request("POST", "/api/v1/organizer/organizations/" + enc(org_id) + "/invites/" + enc(member_id) + "/resend", body=body, **opts)

    def remove_member(self, org_id, member_id, body=None, **opts):
        """Remove a member."""
        return self._c._request("DELETE", "/api/v1/organizer/organizations/" + enc(org_id) + "/members/" + enc(member_id), body=body, **opts)

    def events(self, query=None, **opts):
        """List the organizer's events."""
        return self._c._request("GET", "/api/v1/organizer/events", query=query, **opts)

    def create_event(self, body=None, **opts):
        """Create a draft event."""
        return self._c._request("POST", "/api/v1/organizer/events", body=body, **opts)

    def get_event(self, id, query=None, **opts):
        """Organizer event detail."""
        return self._c._request("GET", "/api/v1/organizer/events/" + enc(id), query=query, **opts)

    def update_event(self, id, body=None, **opts):
        """Update an event."""
        return self._c._request("PUT", "/api/v1/organizer/events/" + enc(id), body=body, **opts)

    def set_event_status(self, id, body=None, **opts):
        """Change an event's status."""
        return self._c._request("PUT", "/api/v1/organizer/events/" + enc(id) + "/status", body=body, **opts)

    def publish_event(self, id, body=None, **opts):
        """Publish a draft event."""
        return self._c._request("POST", "/api/v1/organizer/events/" + enc(id) + "/publish", body=body, **opts)

    def unpublish_event(self, id, body=None, **opts):
        """Unpublish a published event back to draft."""
        return self._c._request("POST", "/api/v1/organizer/events/" + enc(id) + "/unpublish", body=body, **opts)

    def delete_event(self, id, body=None, **opts):
        """Cancel/delete an event."""
        return self._c._request("DELETE", "/api/v1/organizer/events/" + enc(id), body=body, **opts)

    def tickets(self, id, query=None, **opts):
        """List an event's ticket types."""
        return self._c._request("GET", "/api/v1/organizer/events/" + enc(id) + "/tickets", query=query, **opts)

    def create_ticket(self, id, body=None, **opts):
        """Create a ticket type."""
        return self._c._request("POST", "/api/v1/organizer/events/" + enc(id) + "/tickets", body=body, **opts)

    def update_ticket(self, id, tid, body=None, **opts):
        """Update a ticket type."""
        return self._c._request("PUT", "/api/v1/organizer/events/" + enc(id) + "/tickets/" + enc(tid), body=body, **opts)

    def delete_ticket(self, id, tid, body=None, **opts):
        """Delete a ticket type."""
        return self._c._request("DELETE", "/api/v1/organizer/events/" + enc(id) + "/tickets/" + enc(tid), body=body, **opts)

    def schedule(self, id, query=None, **opts):
        """List schedule sessions."""
        return self._c._request("GET", "/api/v1/organizer/events/" + enc(id) + "/schedule", query=query, **opts)

    def create_schedule(self, id, body=None, **opts):
        """Add a schedule session."""
        return self._c._request("POST", "/api/v1/organizer/events/" + enc(id) + "/schedule", body=body, **opts)

    def update_schedule(self, id, sid, body=None, **opts):
        """Update a schedule session."""
        return self._c._request("PUT", "/api/v1/organizer/events/" + enc(id) + "/schedule/" + enc(sid), body=body, **opts)

    def delete_schedule(self, id, sid, body=None, **opts):
        """Delete a schedule session."""
        return self._c._request("DELETE", "/api/v1/organizer/events/" + enc(id) + "/schedule/" + enc(sid), body=body, **opts)

    def sections(self, id, query=None, **opts):
        """List seating/venue sections."""
        return self._c._request("GET", "/api/v1/organizer/events/" + enc(id) + "/sections", query=query, **opts)

    def create_section(self, id, body=None, **opts):
        """Add a section."""
        return self._c._request("POST", "/api/v1/organizer/events/" + enc(id) + "/sections", body=body, **opts)

    def update_section(self, id, sid, body=None, **opts):
        """Update a section."""
        return self._c._request("PUT", "/api/v1/organizer/events/" + enc(id) + "/sections/" + enc(sid), body=body, **opts)

    def delete_section(self, id, sid, body=None, **opts):
        """Delete a section."""
        return self._c._request("DELETE", "/api/v1/organizer/events/" + enc(id) + "/sections/" + enc(sid), body=body, **opts)

    def promo_codes(self, query=None, **opts):
        """List promo codes."""
        return self._c._request("GET", "/api/v1/organizer/promo-codes", query=query, **opts)

    def create_promo_code(self, body=None, **opts):
        """Create a promo code."""
        return self._c._request("POST", "/api/v1/organizer/promo-codes", body=body, **opts)

    def update_promo_code(self, id, body=None, **opts):
        """Update a promo code."""
        return self._c._request("PUT", "/api/v1/organizer/promo-codes/" + enc(id), body=body, **opts)

    def delete_promo_code(self, id, body=None, **opts):
        """Delete a promo code."""
        return self._c._request("DELETE", "/api/v1/organizer/promo-codes/" + enc(id), body=body, **opts)

    def event_analytics(self, id, query=None, **opts):
        """Sales/analytics for an event."""
        return self._c._request("GET", "/api/v1/organizer/events/" + enc(id) + "/analytics", query=query, **opts)

    def event_attendees(self, id, query=None, **opts):
        """Attendee CRM list for an event."""
        return self._c._request("GET", "/api/v1/organizer/events/" + enc(id) + "/attendees", query=query, **opts)

    def event_export(self, id, query=None, **opts):
        """Attendee export CSV (text/csv bytes)."""
        return self._c._request("GET", "/api/v1/organizer/events/" + enc(id) + "/export", query=query, raw=True, **opts)

    def payouts(self, query=None, **opts):
        """List payouts."""
        return self._c._request("GET", "/api/v1/organizer/payouts", query=query, **opts)

    def payout(self, id, query=None, **opts):
        """Payout detail."""
        return self._c._request("GET", "/api/v1/organizer/payouts/" + enc(id), query=query, **opts)

    def request_payout(self, body=None, **opts):
        """Request a payout."""
        return self._c._request("POST", "/api/v1/organizer/payouts/request", body=body, **opts)

    def update_payout_settings(self, body=None, **opts):
        """Update payout settings."""
        return self._c._request("PUT", "/api/v1/organizer/payout-settings", body=body, **opts)

    def refunds(self, org_id, query=None, **opts):
        """List refund requests for an org."""
        return self._c._request("GET", "/api/v1/organizer/orgs/" + enc(org_id) + "/refunds", query=query, **opts)

    def decide_refund(self, id, body=None, **opts):
        """Approve or deny a refund request."""
        return self._c._request("POST", "/api/v1/organizer/refunds/" + enc(id) + "/decide", body=body, **opts)

    def reports(self, org_id, query=None, **opts):
        """List reports filed against an org (read-only)."""
        return self._c._request("GET", "/api/v1/organizer/orgs/" + enc(org_id) + "/reports", query=query, **opts)

    def resolve_report(self, id, body=None, **opts):
        """Resolve a report (admins only; organizers receive 403)."""
        return self._c._request("POST", "/api/v1/organizer/reports/" + enc(id) + "/resolve", body=body, **opts)

    def messages(self, org_id, query=None, **opts):
        """Org-wide buyer message threads."""
        return self._c._request("GET", "/api/v1/organizer/orgs/" + enc(org_id) + "/messages", query=query, **opts)

    def overview(self, org_id, query=None, **opts):
        """Org dashboard overview."""
        return self._c._request("GET", "/api/v1/organizer/orgs/" + enc(org_id) + "/overview", query=query, **opts)

    def referral(self, org_id, query=None, **opts):
        """Referral program summary + commissions."""
        return self._c._request("GET", "/api/v1/organizer/orgs/" + enc(org_id) + "/referral", query=query, **opts)

    def notifications(self, org_id, query=None, **opts):
        """Org notifications."""
        return self._c._request("GET", "/api/v1/organizer/orgs/" + enc(org_id) + "/notifications", query=query, **opts)

    def fund_wallet(self, org_id, body=None, **opts):
        """Top up the org wallet (returns a payment intent)."""
        return self._c._request("POST", "/api/v1/organizer/wallets/org/" + enc(org_id) + "/fund", body=body, **opts)

    def fund_wallet_status(self, org_id, order_id, query=None, **opts):
        """Poll a wallet top-up payment."""
        return self._c._request("GET", "/api/v1/organizer/wallets/org/" + enc(org_id) + "/fund/" + enc(order_id), query=query, **opts)

    def payout_details(self, org_id, query=None, **opts):
        """Stored payout destinations."""
        return self._c._request("GET", "/api/v1/organizer/orgs/" + enc(org_id) + "/payout-details", query=query, **opts)

    def update_payout_details(self, org_id, currency, body=None, **opts):
        """Set payout details for a currency."""
        return self._c._request("PUT", "/api/v1/organizer/orgs/" + enc(org_id) + "/payout-details/" + enc(currency), body=body, **opts)

    def ticket_message(self, ticket_id, body=None, **opts):
        """Reply to a buyer on a ticket thread."""
        return self._c._request("POST", "/api/v1/organizer/tickets/" + enc(ticket_id) + "/message", body=body, **opts)

    def message_thread(self, thread_id, query=None, **opts):
        """A single org message thread."""
        return self._c._request("GET", "/api/v1/organizer/messages/" + enc(thread_id), query=query, **opts)


class WalletsResource:
    """Wallets Organization wallet balances and ledger."""

    def __init__(self, client):
        self._c = client

    def list(self, query=None, **opts):
        """List wallets the caller can see."""
        return self._c._request("GET", "/api/v1/organizer/wallets", query=query, **opts)

    def get(self, org_id, query=None, **opts):
        """An org's wallet balances (per currency)."""
        return self._c._request("GET", "/api/v1/organizer/wallets/org/" + enc(org_id), query=query, **opts)

    def bootstrap(self, org_id, body=None, **opts):
        """Provision an org's wallet."""
        return self._c._request("POST", "/api/v1/organizer/wallets/org/" + enc(org_id) + "/bootstrap", body=body, **opts)

    def transactions(self, org_id, query=None, **opts):
        """Wallet ledger transactions."""
        return self._c._request("GET", "/api/v1/organizer/wallets/org/" + enc(org_id) + "/transactions", query=query, **opts)


class ScannerTokensResource:
    """Scanner tokens Manage short-lived gate-scanner tokens for staff devices."""

    def __init__(self, client):
        self._c = client

    def list(self, org_id, query=None, **opts):
        """List scanner tokens."""
        return self._c._request("GET", "/api/v1/organizer/scanner-tokens/org/" + enc(org_id), query=query, **opts)

    def create(self, org_id, body=None, **opts):
        """Mint a scanner token."""
        return self._c._request("POST", "/api/v1/organizer/scanner-tokens/org/" + enc(org_id), body=body, **opts)

    def update(self, org_id, token_id, body=None, **opts):
        """Update a scanner token."""
        return self._c._request("PUT", "/api/v1/organizer/scanner-tokens/org/" + enc(org_id) + "/" + enc(token_id), body=body, **opts)

    def delete(self, org_id, token_id, body=None, **opts):
        """Revoke a scanner token."""
        return self._c._request("DELETE", "/api/v1/organizer/scanner-tokens/org/" + enc(org_id) + "/" + enc(token_id), body=body, **opts)

    def reissue(self, org_id, token_id, body=None, **opts):
        """Reissue a scanner token's secret."""
        return self._c._request("POST", "/api/v1/organizer/scanner-tokens/org/" + enc(org_id) + "/" + enc(token_id) + "/reissue", body=body, **opts)

    def metrics(self, org_id, token_id, query=None, **opts):
        """Usage metrics for a scanner token."""
        return self._c._request("GET", "/api/v1/organizer/scanner-tokens/org/" + enc(org_id) + "/" + enc(token_id) + "/metrics", query=query, **opts)


class IntegrationsResource:
    """Integrations API keys, MCP tokens, and integration usage metrics."""

    def __init__(self, client):
        self._c = client

    def metrics(self, org_id, query=None, **opts):
        """Integration usage metrics."""
        return self._c._request("GET", "/api/v1/organizer/integrations/org/" + enc(org_id) + "/metrics", query=query, **opts)

    def api_calls(self, org_id, query=None, **opts):
        """Recent API call log."""
        return self._c._request("GET", "/api/v1/organizer/integrations/org/" + enc(org_id) + "/api-calls", query=query, **opts)

    def mcp_calls(self, org_id, query=None, **opts):
        """Recent MCP tool-call log."""
        return self._c._request("GET", "/api/v1/organizer/integrations/org/" + enc(org_id) + "/mcp-calls", query=query, **opts)

    def list_api_keys(self, org_id, query=None, **opts):
        """List API keys."""
        return self._c._request("GET", "/api/v1/organizer/integrations/org/" + enc(org_id) + "/api-keys", query=query, **opts)

    def create_api_key(self, org_id, body=None, **opts):
        """Mint an API key (plaintext returned once)."""
        return self._c._request("POST", "/api/v1/organizer/integrations/org/" + enc(org_id) + "/api-keys", body=body, **opts)

    def update_api_key(self, org_id, key_id, body=None, **opts):
        """Update an API key (scopes, status, allowlist)."""
        return self._c._request("PUT", "/api/v1/organizer/integrations/org/" + enc(org_id) + "/api-keys/" + enc(key_id), body=body, **opts)

    def rotate_api_key(self, org_id, key_id, body=None, **opts):
        """Rotate an API key's secret."""
        return self._c._request("POST", "/api/v1/organizer/integrations/org/" + enc(org_id) + "/api-keys/" + enc(key_id) + "/rotate", body=body, **opts)

    def delete_api_key(self, org_id, key_id, body=None, **opts):
        """Revoke an API key."""
        return self._c._request("DELETE", "/api/v1/organizer/integrations/org/" + enc(org_id) + "/api-keys/" + enc(key_id), body=body, **opts)

    def list_mcp_tokens(self, org_id, query=None, **opts):
        """List MCP tokens."""
        return self._c._request("GET", "/api/v1/organizer/integrations/org/" + enc(org_id) + "/mcp-tokens", query=query, **opts)

    def create_mcp_token(self, org_id, body=None, **opts):
        """Mint an MCP token."""
        return self._c._request("POST", "/api/v1/organizer/integrations/org/" + enc(org_id) + "/mcp-tokens", body=body, **opts)

    def update_mcp_token(self, org_id, token_id, body=None, **opts):
        """Update an MCP token."""
        return self._c._request("PUT", "/api/v1/organizer/integrations/org/" + enc(org_id) + "/mcp-tokens/" + enc(token_id), body=body, **opts)

    def delete_mcp_token(self, org_id, token_id, body=None, **opts):
        """Revoke an MCP token."""
        return self._c._request("DELETE", "/api/v1/organizer/integrations/org/" + enc(org_id) + "/mcp-tokens/" + enc(token_id), body=body, **opts)


class EventCustomizationResource:
    """Event customization Per-event white-label theming."""

    def __init__(self, client):
        self._c = client

    def get(self, event_id, query=None, **opts):
        """Get an event's customization."""
        return self._c._request("GET", "/api/v1/organizer/event-customization/" + enc(event_id), query=query, **opts)

    def update(self, event_id, body=None, **opts):
        """Update an event's customization."""
        return self._c._request("PUT", "/api/v1/organizer/event-customization/" + enc(event_id), body=body, **opts)


class GrowthResource:
    """Growth Comp tickets, CSV import, broadcasts and attendee tags."""

    def __init__(self, client):
        self._c = client

    def mint_comps(self, event_id, body=None, **opts):
        """Issue complimentary tickets."""
        return self._c._request("POST", "/api/v1/organizer/growth/events/" + enc(event_id) + "/comps", body=body, **opts)

    def import_comps_csv(self, event_id, body=None, **opts):
        """Bulk-issue comps from CSV."""
        return self._c._request("POST", "/api/v1/organizer/growth/events/" + enc(event_id) + "/comps/import-csv", body=body, **opts)

    def list_comps(self, event_id, query=None, **opts):
        """List issued comps."""
        return self._c._request("GET", "/api/v1/organizer/growth/events/" + enc(event_id) + "/comps", query=query, **opts)

    def resend_comp(self, event_id, ticket_id, body=None, **opts):
        """Resend a comp ticket email."""
        return self._c._request("POST", "/api/v1/organizer/growth/events/" + enc(event_id) + "/comps/" + enc(ticket_id) + "/resend", body=body, **opts)

    def broadcast_event(self, event_id, body=None, **opts):
        """Broadcast to an event's attendees."""
        return self._c._request("POST", "/api/v1/organizer/growth/events/" + enc(event_id) + "/broadcast", body=body, **opts)

    def broadcast_org(self, org_id, body=None, **opts):
        """Broadcast to an org's followers/subscribers."""
        return self._c._request("POST", "/api/v1/organizer/growth/orgs/" + enc(org_id) + "/broadcast", body=body, **opts)

    def list_broadcasts(self, org_id, query=None, **opts):
        """List sent broadcasts."""
        return self._c._request("GET", "/api/v1/organizer/growth/orgs/" + enc(org_id) + "/broadcasts", query=query, **opts)

    def add_tags(self, body=None, **opts):
        """Tag attendees (body: orgId, ticketIds, tag)."""
        return self._c._request("POST", "/api/v1/organizer/growth/tags", body=body, **opts)

    def remove_tag(self, body=None, **opts):
        """Remove a tag (body: orgId, ticketId, tag)."""
        return self._c._request("DELETE", "/api/v1/organizer/growth/tags", body=body, **opts)

    def list_tags(self, org_id, query=None, **opts):
        """List tags for an org."""
        return self._c._request("GET", "/api/v1/organizer/growth/orgs/" + enc(org_id) + "/tags", query=query, **opts)

    def tag_attendees(self, org_id, tag, query=None, **opts):
        """List attendees with a given tag."""
        return self._c._request("GET", "/api/v1/organizer/growth/orgs/" + enc(org_id) + "/tags/" + enc(tag) + "/attendees", query=query, **opts)


class PublicEventsResource:
    """Public events (vanity) Public org/event read endpoints used by hosted pages and white-label sites."""

    def __init__(self, client):
        self._c = client

    def get_by_slug(self, slug, query=None, **opts):
        """Public event by slug."""
        return self._c._request("GET", "/api/v1/public/events/" + enc(slug), query=query, **opts)

    def org_events(self, org_id, query=None, **opts):
        """An organization's public events."""
        return self._c._request("GET", "/api/v1/public/events/orgs/by/" + enc(org_id), query=query, **opts)

    def get_by_org_event(self, org_id, event_id, query=None, **opts):
        """Public event by org + event id."""
        return self._c._request("GET", "/api/v1/public/events/by/" + enc(org_id) + "/" + enc(event_id), query=query, **opts)

    def get_by_id(self, event_id, query=None, **opts):
        """Public event by id."""
        return self._c._request("GET", "/api/v1/public/events/by-id/" + enc(event_id), query=query, **opts)

    def preview(self, event_id, query=None, **opts):
        """Draft event preview (token-gated)."""
        return self._c._request("GET", "/api/v1/public/events/preview/" + enc(event_id), query=query, **opts)


class SiteResource:
    """Public site Sitemap, newsletter signup and platform status."""

    def __init__(self, client):
        self._c = client

    def sitemap(self, query=None, **opts):
        """Sitemap XML (application/xml bytes)."""
        return self._c._request("GET", "/api/v1/public/sitemap.xml", query=query, raw=True, **opts)

    def newsletter_start(self, body=None, **opts):
        """Begin newsletter double-opt-in."""
        return self._c._request("POST", "/api/v1/public/newsletter/start", body=body, **opts)

    def newsletter_confirm(self, body=None, **opts):
        """Confirm a newsletter subscription."""
        return self._c._request("POST", "/api/v1/public/newsletter", body=body, **opts)

    def status(self, query=None, **opts):
        """Platform status + incidents."""
        return self._c._request("GET", "/api/v1/public/status", query=query, **opts)


class CommunityResource:
    """Community Verified-attendee reviews, organizer follows and event waitlists."""

    def __init__(self, client):
        self._c = client

    def submit_review(self, body=None, **opts):
        """Submit a verified-attendee review (ticketCode + email prove ownership)."""
        return self._c._request("POST", "/api/v1/community/reviews", body=body, **opts)

    def org_reviews(self, org_id, query=None, **opts):
        """Published reviews for an org."""
        return self._c._request("GET", "/api/v1/community/orgs/" + enc(org_id) + "/reviews", query=query, **opts)

    def event_reviews(self, event_id, query=None, **opts):
        """Published reviews for an event."""
        return self._c._request("GET", "/api/v1/community/events/" + enc(event_id) + "/reviews", query=query, **opts)

    def follow(self, org_id, body=None, **opts):
        """Follow an organizer."""
        return self._c._request("POST", "/api/v1/community/orgs/" + enc(org_id) + "/follow", body=body, **opts)

    def unfollow(self, token, query=None, **opts):
        """Unsubscribe via emailed token."""
        return self._c._request("GET", "/api/v1/community/unfollow/" + enc(token), query=query, **opts)

    def join_waitlist(self, event_id, body=None, **opts):
        """Join an event waitlist."""
        return self._c._request("POST", "/api/v1/community/events/" + enc(event_id) + "/waitlist", body=body, **opts)

    def accept_waitlist(self, id, token, query=None, **opts):
        """Accept a waitlist offer via emailed token."""
        return self._c._request("GET", "/api/v1/community/waitlist/accept/" + enc(id) + "/" + enc(token), query=query, **opts)

    def manage_reviews(self, org_id, query=None, **opts):
        """Organizer view of all reviews (incl. pending)."""
        return self._c._request("GET", "/api/v1/community/orgs/" + enc(org_id) + "/reviews/manage", query=query, **opts)

    def reply_review(self, id, body=None, **opts):
        """Organizer reply to a review."""
        return self._c._request("POST", "/api/v1/community/reviews/" + enc(id) + "/reply", body=body, **opts)

    def set_review_status(self, id, body=None, **opts):
        """Publish/hide a review."""
        return self._c._request("POST", "/api/v1/community/reviews/" + enc(id) + "/status", body=body, **opts)

    def followers(self, org_id, query=None, **opts):
        """List an org's followers."""
        return self._c._request("GET", "/api/v1/community/orgs/" + enc(org_id) + "/followers", query=query, **opts)

    def remove_follower(self, org_id, follower_id, body=None, **opts):
        """Remove a follower."""
        return self._c._request("DELETE", "/api/v1/community/orgs/" + enc(org_id) + "/followers/" + enc(follower_id), body=body, **opts)

    def event_waitlist(self, event_id, query=None, **opts):
        """List an event's waitlist."""
        return self._c._request("GET", "/api/v1/community/events/" + enc(event_id) + "/waitlist", query=query, **opts)

    def offer_waitlist(self, event_id, body=None, **opts):
        """Offer spots to waitlisted attendees."""
        return self._c._request("POST", "/api/v1/community/events/" + enc(event_id) + "/waitlist/offer", body=body, **opts)


class TrackResource:
    """Tracking Lightweight page-view tracking."""

    def __init__(self, client):
        self._c = client

    def view(self, body=None, **opts):
        """Record a page view."""
        return self._c._request("POST", "/api/v1/track/view", body=body, **opts)


class WebhooksResource:
    """Webhooks Register webhook endpoints and inspect/replay deliveries. (Use client.webhooks.verify() to validate inbound signatures.)"""

    def __init__(self, client):
        self._c = client

    def catalog(self, query=None, **opts):
        """List subscribable event types."""
        return self._c._request("GET", "/api/v1/webhooks/catalog", query=query, **opts)

    def list(self, query=None, **opts):
        """List registered webhook endpoints."""
        return self._c._request("GET", "/api/v1/webhooks", query=query, **opts)

    def create(self, body=None, **opts):
        """Register a webhook endpoint."""
        return self._c._request("POST", "/api/v1/webhooks", body=body, **opts)

    def update(self, id, body=None, **opts):
        """Update a webhook endpoint."""
        return self._c._request("PUT", "/api/v1/webhooks/" + enc(id), body=body, **opts)

    def rotate_secret(self, id, body=None, **opts):
        """Rotate a webhook signing secret."""
        return self._c._request("POST", "/api/v1/webhooks/" + enc(id) + "/rotate-secret", body=body, **opts)

    def delete(self, id, body=None, **opts):
        """Delete a webhook endpoint."""
        return self._c._request("DELETE", "/api/v1/webhooks/" + enc(id), body=body, **opts)

    def deliveries(self, id, query=None, **opts):
        """List delivery attempts for an endpoint."""
        return self._c._request("GET", "/api/v1/webhooks/" + enc(id) + "/deliveries", query=query, **opts)

    def test(self, id, body=None, **opts):
        """Send a test event to an endpoint."""
        return self._c._request("POST", "/api/v1/webhooks/" + enc(id) + "/test", body=body, **opts)

    def replay(self, id, body=None, **opts):
        """Replay a past delivery."""
        return self._c._request("POST", "/api/v1/webhooks/deliveries/" + enc(id) + "/replay", body=body, **opts)


class WhiteLabelResource:
    """White-label Self-contained white-label surface: events, ticket types, customization, orders, tickets and check-in under one integration."""

    def __init__(self, client):
        self._c = client

    def me(self, query=None, **opts):
        """The integration's white-label context."""
        return self._c._request("GET", "/api/v1/white-label/me", query=query, **opts)

    def events(self, query=None, **opts):
        """List white-label events."""
        return self._c._request("GET", "/api/v1/white-label/events", query=query, **opts)

    def create_event(self, body=None, **opts):
        """Create a white-label event."""
        return self._c._request("POST", "/api/v1/white-label/events", body=body, **opts)

    def get_event(self, id, query=None, **opts):
        """White-label event detail."""
        return self._c._request("GET", "/api/v1/white-label/events/" + enc(id), query=query, **opts)

    def update_event(self, id, body=None, **opts):
        """Update a white-label event."""
        return self._c._request("PUT", "/api/v1/white-label/events/" + enc(id), body=body, **opts)

    def delete_event(self, id, body=None, **opts):
        """Delete a white-label event."""
        return self._c._request("DELETE", "/api/v1/white-label/events/" + enc(id), body=body, **opts)

    def ticket_types(self, event_id, query=None, **opts):
        """List ticket types."""
        return self._c._request("GET", "/api/v1/white-label/events/" + enc(event_id) + "/ticket-types", query=query, **opts)

    def create_ticket_type(self, event_id, body=None, **opts):
        """Create a ticket type."""
        return self._c._request("POST", "/api/v1/white-label/events/" + enc(event_id) + "/ticket-types", body=body, **opts)

    def get_customization(self, event_id, query=None, **opts):
        """Get event customization."""
        return self._c._request("GET", "/api/v1/white-label/events/" + enc(event_id) + "/customization", query=query, **opts)

    def update_customization(self, event_id, body=None, **opts):
        """Update event customization."""
        return self._c._request("PUT", "/api/v1/white-label/events/" + enc(event_id) + "/customization", body=body, **opts)

    def orders(self, query=None, **opts):
        """List white-label orders."""
        return self._c._request("GET", "/api/v1/white-label/orders", query=query, **opts)

    def tickets(self, query=None, **opts):
        """List white-label tickets."""
        return self._c._request("GET", "/api/v1/white-label/tickets", query=query, **opts)

    def checkin_scan(self, body=None, **opts):
        """Validate a ticket at a white-label gate."""
        return self._c._request("POST", "/api/v1/white-label/checkin/scan", body=body, **opts)

    def checkin_stats(self, event_id, query=None, **opts):
        """White-label check-in stats."""
        return self._c._request("GET", "/api/v1/white-label/checkin/stats/" + enc(event_id), query=query, **opts)

    def wallets(self, query=None, **opts):
        """White-label wallet balances."""
        return self._c._request("GET", "/api/v1/white-label/wallets", query=query, **opts)


class SupportResource:
    """Support Support tickets and threaded messages."""

    def __init__(self, client):
        self._c = client

    def list(self, query=None, **opts):
        """List support tickets."""
        return self._c._request("GET", "/api/v1/support", query=query, **opts)

    def create(self, body=None, **opts):
        """Open a support ticket."""
        return self._c._request("POST", "/api/v1/support", body=body, **opts)

    def get(self, id, query=None, **opts):
        """Support ticket detail."""
        return self._c._request("GET", "/api/v1/support/" + enc(id), query=query, **opts)

    def send_message(self, id, body=None, **opts):
        """Reply on a support ticket."""
        return self._c._request("POST", "/api/v1/support/" + enc(id) + "/messages", body=body, **opts)


class UtilResource:
    """Utilities Helper endpoints."""

    def __init__(self, client):
        self._c = client

    def resolve_coords(self, body=None, **opts):
        """Resolve an address to coordinates."""
        return self._c._request("POST", "/api/v1/util/resolve-coords", body=body, **opts)


RESOURCES = {
    "auth": AuthResource,
    "users": UsersResource,
    "saved_searches": SavedSearchesResource,
    "data_export": DataExportResource,
    "events": EventsResource,
    "tickets": TicketsResource,
    "orders": OrdersResource,
    "payments": PaymentsResource,
    "checkin": CheckinResource,
    "scan": ScanResource,
    "search": SearchResource,
    "media": MediaResource,
    "organizer": OrganizerResource,
    "wallets": WalletsResource,
    "scanner_tokens": ScannerTokensResource,
    "integrations": IntegrationsResource,
    "event_customization": EventCustomizationResource,
    "growth": GrowthResource,
    "public_events": PublicEventsResource,
    "site": SiteResource,
    "community": CommunityResource,
    "track": TrackResource,
    "webhooks": WebhooksResource,
    "white_label": WhiteLabelResource,
    "support": SupportResource,
    "util": UtilResource,
}
